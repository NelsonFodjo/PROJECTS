export default function taskReducer(state, action){
    switch(action.type){
        case "ADD_TASK": {
            const newTask = {
                id : Date.now(),
                text : action.
            }
        }
            
        default:
            return state;
    }
}