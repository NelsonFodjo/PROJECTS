import React, { useReducer } from "react";

function taskReducer(){

}

export default function App(){
  consinitialState = {tasks: []}
  const [state, dispatch] = useReducer(taskReducer, initialState);
  
  return(
    <>
    <h1>Hi Nelson</h1>
    </>
  )
}